// Define a firstNumber and secondNumber variables
// Assign each variable a number type value
// Code the following operations:
// Adding both variables
// Subtracting both variables
// Multiplying both variables
// Dividing both variables
// Remainder from both variables
// For each operation show the following output:
// Example: I add 2 + 3 and the result is 5 (where 2 and 3 are firstNumber and secondNumber and 5 it's the operation result)
// Use a template variable for each output and console.log()


const firstNumber = 20;
const secondNumber = 2;
const add = `I add ${firstNumber} + ${secondNumber} and the result is ` ;
const sub = `I subtract ${firstNumber} - ${secondNumber} and the result is ` ;
const multiply = `I multiply ${firstNumber} * ${secondNumber} and the result is ` ;
const div = `I divide ${firstNumber} / ${secondNumber} and the result is ` ;
const reminder = `I divede ${firstNumber} % ${secondNumber} and the reminder is ` ;

console.log('------------------------------------------------------------------------')
console.log(add ,  firstNumber + secondNumber); // 0
console.log(sub ,  firstNumber - secondNumber); // 0
console.log(multiply ,  firstNumber * secondNumber); // 0
console.log(div ,  firstNumber / secondNumber); // 0
console.log(reminder ,  firstNumber % secondNumber); // 0
console.log('------------------------------------------------------------------------')
